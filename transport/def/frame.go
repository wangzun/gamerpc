package def

//---------------------------------------------------------------------------
// 帧结构
// uint32(streamId) + uint8(flag) + header(可选) + setting(可选) + data(可选)
//---------------------------------------------------------------------------
